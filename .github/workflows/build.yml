name: Build Phar

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: Extract ZIP
        run: unzip EconomyShopFinal.zip

      - name: Build Phar
        run: |
          php -d phar.readonly=0 -r "
          \$phar = new Phar('EconomyShop.phar');
          \$phar->startBuffering();
          \$phar->setStub('<?php __HALT_COMPILER();');
          \$phar->buildFromDirectory('EconomyShopFinal');
          \$phar->stopBuffering();
          echo 'EconomyShop.phar ban gaya!';
          "

      - name: Upload Phar
        uses: actions/upload-artifact@v4
        with:
          name: EconomyShop
          path: EconomyShop.phar
